# دليل حل المشاكل - Troubleshooting Guide

## المشكلة: شاشة بيضاء / White Screen

### الأسباب المحتملة والحلول:

#### 1. ⚠️ قاعدة البيانات غير مُعدّة (السبب الأكثر شيوعاً)

**المشكلة:**
- لم يتم تنفيذ SQL script
- الجداول غير موجودة في Supabase

**الحل:**
1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. اذهب إلى **SQL Editor**
3. افتح ملف `SETUP_DATABASE.sql`
4. انسخ **كل المحتوى**
5. الصقه والصق **Run** أو **F5**
6. انتظر رسالة "Success" ✅

#### 2. ⚠️ Email Authentication غير مفعل

**المشكلة:**
- Email provider معطل في Supabase
- Email confirmation مفعل

**الحل:**
1. اذهب إلى `Authentication > Providers`
2. فعّل **Email** provider
3. اذهب إلى `Authentication > Settings`
4. عطّل **"Enable email confirmations"**

#### 3. ⚠️ متغيرات البيئة مفقودة

**المشكلة:**
- `.env` file غير موجود
- المتغيرات غير محملة

**الحل - محلياً (Local):**
تأكد من وجود ملف `.env` في جذر المشروع:
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

**الحل - Netlify:**
1. اذهب إلى Netlify Dashboard
2. Site Settings > Environment Variables
3. أضف:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. أعد النشر (Redeploy)

#### 4. 🔍 كيفية التحقق من المشكلة:

افتح **Developer Console** (F12):

```javascript
// في Console، اكتب:
console.log(import.meta.env.VITE_SUPABASE_URL)
console.log(import.meta.env.VITE_SUPABASE_ANON_KEY)
```

**إذا ظهرت المتغيرات:**
- المشكلة ليست في المتغيرات
- افحص أخطاء JavaScript في Console

**إذا ظهرت `undefined`:**
- المتغيرات مفقودة
- أضفها في `.env` (محلي) أو Netlify (منتج)

### 5. 🛠️ Error Boundary

الآن يوجد Error Boundary سيظهر رسالة خطأ بدلاً من الشاشة البيضاء:

**ماذا سترى:**
- رسالة "Something went wrong"
- تفاصيل الخطأ
- زر "Reload Page"

**ماذا تفعل:**
1. اقرأ رسالة الخطأ
2. افتح Console (F12) لمزيد من التفاصيل
3. اتبع التعليمات أدناه حسب نوع الخطأ

### 6. 📋 أخطاء شائعة ورسائلها:

#### "Missing Supabase environment variables"
```
الحل: أضف المتغيرات في .env أو Netlify
```

#### "relation 'profiles' does not exist"
```
الحل: نفذ SETUP_DATABASE.sql في Supabase
```

#### "Invalid API key"
```
الحل: تحقق من VITE_SUPABASE_ANON_KEY
```

#### "Failed to fetch"
```
الحل: تحقق من اتصال الإنترنت و VITE_SUPABASE_URL
```

### 7. ✅ خطوات التحقق السريعة:

قم بهذه الخطوات بالترتيب:

- [ ] تنفيذ `SETUP_DATABASE.sql` في Supabase
- [ ] تفعيل Email Auth في Supabase
- [ ] تعطيل Email Confirmation
- [ ] التأكد من `.env` موجود ويحتوي على المتغيرات
- [ ] افتح Console (F12) واقرأ الأخطاء
- [ ] أعد تحميل الصفحة (Ctrl+Shift+R)

### 8. 🔧 للمطورين:

**تشغيل محلي (Local):**
```bash
# تثبيت
npm install

# تشغيل
npm run dev

# بناء
npm run build
```

**فحص Build:**
```bash
npm run build
# إذا نجح البناء، المشكلة في runtime
# إذا فشل، المشكلة في الكود
```

### 9. 📱 مشاكل خاصة بالموبايل:

**الشاشة لا تملأ المساحة:**
- تم إصلاحها! CSS يدعم viewport الآن

**التطبيق لا يعمل offline:**
- Service Worker مفعل، لكن يحتاج تسجيل دخول أولاً

**الشاشة تهتز عند Scroll:**
- تم إصلاحها! overscroll معطل

### 10. 🆘 إذا استمرت المشكلة:

1. **احذف cache المتصفح:**
   - Ctrl+Shift+Delete
   - اختر "Cached images and files"
   - Clear

2. **جرب Incognito Mode:**
   - Ctrl+Shift+N (Chrome)
   - Ctrl+Shift+P (Firefox)

3. **تحقق من Supabase Status:**
   - https://status.supabase.com

4. **افحص Network Tab:**
   - F12 > Network
   - أعد تحميل
   - ابحث عن Failed requests (أحمر)

### 11. 📞 معلومات للدعم:

إذا احتجت مساعدة، وفر:
- لقطة شاشة من Console (F12)
- لقطة شاشة من Network Tab
- رسالة الخطأ كاملة
- URL الموقع
- المتصفح والإصدار

---

## ملخص الحل السريع:

**99% من المشاكل تُحل بـ:**

```
✅ تنفيذ SETUP_DATABASE.sql
✅ تفعيل Email Auth
✅ التأكد من .env موجود
✅ إعادة تحميل الصفحة
```

**إذا استمرت المشكلة:**
- افتح F12
- اقرأ الأخطاء
- اتبع التعليمات أعلاه
